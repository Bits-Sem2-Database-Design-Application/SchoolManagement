function submitCloseChangeRequestForm(form, getRemarks, setRemarks) {
    setRemarks.value = getRemarks.value;
    form.submit();
}

function submiteEditStudentDetailsForm(form) {
    form.submit();
}
